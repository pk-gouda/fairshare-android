<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for API 31+ (Android 12+).
    Controls both cloud backup (Google Account) and device-to-device transfer.

    android:allowBackup="false" disables cloud backup, but device-to-device
    transfer on API 31+ is governed separately by this file. Both are fully
    excluded here: all valid domains are excluded so no data is transferred
    in either direction.

    Cache directories are excluded by the system automatically — no explicit
    rule needed. External app files are covered by domain="external".
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database"   path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file"       path="." />
        <exclude domain="root"       path="." />
        <exclude domain="external"   path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database"   path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file"       path="." />
        <exclude domain="root"       path="." />
        <exclude domain="external"   path="." />
    </device-transfer>
</data-extraction-rules>