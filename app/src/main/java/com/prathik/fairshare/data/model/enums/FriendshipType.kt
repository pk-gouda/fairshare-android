package com.prathik.fairshare.data.model.enums

/**
 * How the friendship was formed.
 *
 * DIRECT — users sent/accepted a direct friend request
 * GROUP  — users met through a shared group
 */
enum class FriendshipType {
    DIRECT,
    GROUP
}